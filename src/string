/* vim: set ft=cpp: */

#include <exception>
/*
auto string::operator [](usize index) -> i8& {
	if(index >= this->len)
	{
		throw new exception::out_of_bounds(
				string("the len is ")
				<< this->len 
				<< string(" but the index is ") 
				<< index
				);
	}

	return this->data[index];
}*/
